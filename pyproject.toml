[project]
authors = [{name = "kolikaran"}]
description = ""
name = "voice-forge"
requires-python = ">=3.10,<4.0"
version = "0.1.0"

dependencies = [
  "dynaconf>=3.2.12,<4.0.0",
  "jinja2>=3.1.6,<4.0.0",
  "pytz>=2025.2,<2026.0",
  "pytest>=9.0.1,<10.0.0",
  "crongle @ git+https://github.com/kolikaran1992/crongle.git",
  "kagglehub>=0.3.13,<0.4.0",
  "librosa>=0.10.2",
  "phonemizer>=3.3.0",
  "tensorboard>=2.0,<3.0",
  "world4py>=0.1.1,<0.2.0",
  "unidecode>=1.4.0,<2.0.0", # Kaggle-safe pins
  "numpy<=1.26.4",
  "pandas<=2.2.3",
  "matplotlib<=3.7.2",
  "scikit-learn<=1.2.2",
  "scipy<=1.15.3", # Kaggle GPU stack
  "torch==2.6.0",
  "torchaudio==2.6.0",
  "torchvision==0.21.0",
  "huggingface-hub<1.0",
  "cython (>=3.2.2,<4.0.0)",
]

# [build-system]
# build-backend = "poetry.core.masonry.api"
# requires = ["poetry-core>=1.6.0", "setuptools>=61.0", "wheel", "Cython>=3.0"]

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61.0", "wheel", "Cython>=3.0"]
