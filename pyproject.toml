[project]
name = "voice-forge"
version = "0.1.0"
description = ""
authors = [
    {name = "kolikaran"}
]
readme = "README.md"
requires-python = ">=3.10, <4.0"
dependencies = [
    "dynaconf (>=3.2.12,<4.0.0)",
    "jinja2 (>=3.1.6,<4.0.0)",
    "pytz (>=2025.2,<2026.0)",
    "pytest (>=9.0.1,<10.0.0)",
    "huggingface-hub (>=1.1.5,<2.0.0)",
    "pandas (>=2.3.3,<3.0.0)",
    "crongle @ git+ssh://git@github.com/kolikaran1992/crongle.git"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
black = "^25.11.0"
isort = "^7.0.0"

